# Используется при сборке фронтенда (yarn build).
# Бэкенд сам раздаёт собранный фронтенд на том же адресе, поэтому указываем локальный сервер.
REACT_APP_BACKEND_URL=http://127.0.0.1:8001
# Десктоп-сборка: пропускаем стартовую (веб) страницу и открываем приложение сразу.
REACT_APP_IS_DESKTOP=1
